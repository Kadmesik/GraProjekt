_mainSurface.FillWithRandomGarbage(_mainSurface.Font);

_mainSurface.Fill(new Rectangle(30, 0, 20, 5), Color.Violet, Color.Black, 0, Mirror.None);

Thread.Sleep(1000);


public override bool ProcessKeyboard(Keyboard keyboard)
    {
        bool handled = false;


        if (keyboard.IsKeyPressed(SadConsole.Input.Keys.Enter))
        {

        SadConsole.Game.Instance.Screen = new MenuScreen();

        }
        return handled;
    }